# Third Party Licenses

Runtime dependencies are selected from permissive or institution-friendly
licenses where possible. Run the license review locally with:

```bash
uv run pip-licenses --format=markdown --with-license-file
```

No GPL dependency is intentionally used in the core package.
