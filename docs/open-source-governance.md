# Open Source Governance

The project uses Apache-2.0. Implementation work happens on `develop`, and
changes move to `master` through pull requests.

Core dependencies should use permissive or institution-friendly licenses.
